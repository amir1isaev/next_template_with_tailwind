import { interfaceState } from '../../state'
import { IInterfaceState, InterfaceAction } from '../../type/interface'

const interfaceReducer = (state = interfaceState, action: InterfaceAction): IInterfaceState => {
	switch ('') {
		default:
			return state
	}
}

export default interfaceReducer
